> 本文件暂存弃用但未来可能采用的描述性文字

KSFS：每一层文件夹维护一份目录文件，不遗漏地记录当前目录下的每一个文件与文件夹的介绍性内容。每一个实体的文件（xxx.md）头部用 YAML 写元数据，同时记录 relations（target + type），为知识图谱功能做准备。

HSI：首要职责是维护索引缓存，自动分配实体id、记录实体名、确定性标签（type）、提示性标签（tags）、文件路径、面包屑路径、relations、自身的简介（来自md文件头部）、组装后的路径简介（来自一路上的目录文件），同时记录 KSFS 中文件哈希与变更时间，以便快速判断哪个文件被改过、需要更新 SVS 与 KG。

### 设定导入的Prompt设计：
以“设定导入”为例，它的Prompt模板的组成应该如下：
第一步（用户已经点击了“设定导入”技能，CB准备好这个模板，等待用户的输入）：通用开头[“你是XXX”, JSON-only规则, 全局规定与偏好 ] + 技能要求[“现在需要你导入设定，按照以下模板：...”, 草稿模板, 技能规定与偏好（例如划分后先逐条检索，KSFS已有设定标记为重复）] + 工具列表[检索, 文件写入, 草稿晋升, ...] + 用户刚刚输入的内容（完整的、需要导入的设定，可能已经经过预处理：Agent粗划分 / 用户在Agent的提示下按一定规范输入） + 返回规则与历史记录[“在主要部分的结尾你还需要维护一个列表：设定文件名称 | 状态，状态只有以下三种：待验收、草稿、已保存、重复，等待用户指令。”, 空列表, 返回规则（可能需要单独的ReAct范式）]
LLM可能
Agent根据本侧的规定，将LLM返回的内容分析、拆解、保存草稿，从系统的资源管理器打开workspace（草稿区），显示提示性文字（请核查草稿内容并给出反馈...）并渲染列表（此时每一项应该都是待验收），此时用户可以选择每一项。由于Agent无法关心用户是否人工打开文件，
第二步（LLM返回了划分好的草稿，可能是极大篇幅的JSON内容，Agent已经根据一定规则拆分了返回的结果）：通用开头[“你是XXX”, JSON-only规则, 全局规定与偏好 ] + 技能要求[“现在需要你导入设定，按照以下模板：...”, 草稿模板, 技能规定与偏好] + 工具列表[检索, 文件写入, 草稿晋升, ...] + 用户刚刚输入的内容（可能已经经过预处理：Agent粗划分 / 用户在Agent的提示下按一定规范输入） + 历史记录与规则[规则:“你需要返回一个列表：设定名称 | 状态”, 第一次返回]