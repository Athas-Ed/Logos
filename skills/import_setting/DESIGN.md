# import_setting — 设定导入（产品 Skill）

- **范式**：`pipeline`（进程内 `PipelineRunner`，非 MCP 包）
- **profile**：`default_import_v0`（`resources/entity_template/` + `resources/pipelines/`）
- **落盘**：`workspace/setting_entry/`（`render_spec` 路径规则）
- **规格封存**：`original_docs/重要子系统开发文档/设定导入Skill开发.md`
- **重叠 / 晋升**：F6-08（`pipeline_warning`、`DraftPromotionPort`）
